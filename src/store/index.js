import { createStore } from 'vuex'
import basicsBlock from '../content/basicsBlock'

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    basics: basicsBlock
  }
})
